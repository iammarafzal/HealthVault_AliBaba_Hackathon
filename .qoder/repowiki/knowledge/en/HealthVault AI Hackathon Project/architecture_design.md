The repository is organized as a monorepo where the `backend` child provides the FastAPI REST API layer (authentication, medical records, voice/OCR, multi-agent extraction) and other sibling children implement the frontend, deployment scripts, or shared configuration; cross-child communication follows a client-server model with the backend exposing HTTP endpoints consumed by the frontend and Alibaba Cloud services invoked from within the backend.